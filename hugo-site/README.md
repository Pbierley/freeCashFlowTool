# Hugo site for freeCashFlowTool docs

This is a minimal Hugo site for the GitHub Pages landing page.

Building locally:

```bash
hugo -s hugo-site -d docs
```

This outputs static files to `docs/` which the GitHub Actions workflow will deploy.
